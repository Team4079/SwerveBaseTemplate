package frc.robot.utils.controller

/**
 * Object representing the triggers on a controller.
 * Provides constants for the left and right triggers.
 */
object Trigger {
    /** Constant for the LEFT trigger (2). */
    const val LEFT = 2

    /** Constant for the RIGHT trigger (3). */
    const val RIGHT = 3
}
