package frc.robot.utils.controller

/**
 * Object representing the analog axes on a controller.
 * Provides constants for the various analog stick axes.
 */
object Axis {
    /** Constant for the LEFT_ANALOG_X axis (0). */
    const val LEFT_ANALOG_X = 0

    /** Constant for the LEFT_ANALOG_Y axis (1). */
    const val LEFT_ANALOG_Y = 1

    /** Constant for the RIGHT_ANALOG_X axis (4). */
    const val RIGHT_ANALOG_X = 4

    /** Constant for the RIGHT_ANALOG_Y axis (5). */
    const val RIGHT_ANALOG_Y = 5
}
